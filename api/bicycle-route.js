
export default async function handler(req,res){if(req.method!=='GET')return res.status(405).json({error:'Method Not Allowed'});const key=process.env.KAKAO_REST_API_KEY;if(!key)return res.status(500).json({error:'KAKAO_REST_API_KEY 환경변수가 없습니다.'});const {start_x,start_y,end_x,end_y}=req.query;if(!start_x||!start_y||!end_x||!end_y)return res.status(400).json({error:'출발/도착 좌표가 필요합니다.'});const defs=[['BIKE_ONLY','자전거도로 우선'],['SHORTEST','최단 경로'],['ACCESSIBLE','편안한길']];const one=async([mode,label])=>{const p=new URLSearchParams({start_x,start_y,end_x,end_y,route_mode:mode,input_coord:'WGS84',output_coord:'WGS84'});const r=await fetch('https://dapi.kakao.com/v2/routing/bicycle?'+p,{headers:{Authorization:'KakaoAK '+key}});const d=await r.json();if(!r.ok||!d.route)return null;return {routeMode:mode,label,totalDistance:d.route.properties?.totalDistance||0,totalTime:d.route.properties?.totalTime||0,route:d.route}};const routes=(await Promise.all(defs.map(one))).filter(Boolean);res.status(200).json({routes})}
