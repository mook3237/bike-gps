// 네비게이션 기능 (향후 확장)
class BikeNavigation {
    constructor() {
        this.destination = null;
        this.currentMode = 'single';
    }
}

const bikeNav = new BikeNavigation();
if (typeof diagnostic !== 'undefined') {
    diagnostic.add('bikeNav 생성됨');
}
